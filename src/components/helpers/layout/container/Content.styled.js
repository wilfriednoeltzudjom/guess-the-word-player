import styled from 'styled-components';

import dimensions from '../../../../config/dimensions';

export default styled.div`
  flex: 1;
  padding-bottom: ${dimensions.footer.minHeight};
  display: flex;
  flex-direction: column;
`;
